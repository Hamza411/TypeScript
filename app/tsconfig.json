{
    "extends": "../tsconfig.base",
    "compilerOptions": {
        "removeComments": true
    },
    "include": [
        // "./**/.ts" //thorugh all sub directory looks all files with .ts
        "./**/*"
    ]
}